"use client";

// COMPONENTE SILENCIADO GLOBALMENTE
// El usuario reportó duplicidad con el Navbar global.
// Este archivo se mantiene para no romper importaciones, pero no renderiza nada.

export default function Masthead() {
    return null;
}
